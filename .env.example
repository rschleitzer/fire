# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/fhir

# Server Configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=3000

# SQLx Configuration
# Set SQLX_OFFLINE=true to build without a database connection (uses .sqlx/ offline metadata)
# Set SQLX_OFFLINE=false when you need to regenerate query metadata with `cargo sqlx prepare`
SQLX_OFFLINE=false

# Database Pool Configuration (Optional)
# DB_MAX_CONNECTIONS=10
# DB_MIN_CONNECTIONS=2
# DB_CONNECTION_TIMEOUT_SECS=30
# DB_IDLE_TIMEOUT_SECS=600

# Logging Configuration (Optional)
# RUST_LOG=fire=info,tower_http=info,sqlx=warn
